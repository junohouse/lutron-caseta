# `leap` is the TLS protocol every current bridge speaks, and needs a paired certificate.
[workspace]
resolver = "3"
members = ["leap"]

[profile.release]
opt-level = "z"
codegen-units = 1
lto = true
strip = true
